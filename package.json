{
  "name": "first-to-ten-root",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently --kill-others --names \"backend,frontend\" --prefix-colors \"bgMagenta.bold,bgCyan.bold\" \"npm run dev-backend\" \"npm run dev-frontend\""
  }
}
